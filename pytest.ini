[pytest]
addopts = -ra -q --strict-markers --browser chromium --browser-channel chrome
testpaths = tests
markers =
    smoke: Critical healthcare smoke tests
    regression: Broader healthcare regression tests
